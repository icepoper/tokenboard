# 更新日志

[English](CHANGELOG.en.md) | [简体中文](CHANGELOG.md)

本文件记录 TokenBoard 的版本更新内容，是 GitHub Releases 发布说明的源文件（格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)）。

## [Unreleased]

（暂无）

## [0.2.0] - 2026-08-07

### 新增

- **多服务商架构**：新增服务商抽象层与 ProviderManager，菜单栏 / 弹出面板 / 设置页支持在千问与 DeepSeek 之间切换，选择持久化
- **DeepSeek 监控**：平台会话认证（userToken，Keychain 安全存储），实时展示余额（充值 / 赠金 / 总余额）、今日与本月 tokens / 消费 / 请求数
- **官方 logo 服务商切换**：弹出面板与设置页用千问 / DeepSeek 官方 logo 一键切换服务商
- **近 7 天按模型 token 趋势图**：每个产生调用的模型渲染一张趋势图（多模型多图），月初自动跨月合并数据
- **低余额预警通知**：余额低于 20 元时发送系统通知，余额回升后再跌破可再次提醒
- **开发体验优化**：改用固定自签名证书签名，钥匙串授权一次永久生效（不再每次构建弹窗）

### 质量

- 单元测试 56/56 通过（新增 DeepSeek 解析与多服务商用例）

## [0.1.5] - 2026-08-07

### 修复

- **usage 接口稀疏响应兼容**：千问 usage 接口对无消耗的窗口可能只返回部分字段（实测仅返回 `per1WeekPercentage`），此前会误报「usage 响应字段缺失」导致面板显示「数据更新失败」；现缺失字段按未知（`--`）展示，不再报错
- 5h / 7d 重置时间缺失时展示 `--`，额度预警在数据缺失时自动跳过
- **英文模式「剩余 --%」回退中文修复**：用量未知时 5h 限额文案在英文下显示中文「剩余 --%」，补充 Localizable.strings key（en: `Remaining --%`）

### 质量

- 单元测试 38/38 通过（新增稀疏响应解析 + 本地化用例）

## [0.1.4] - 2026-08-05

### 新增

- **国际化（中英文）**：应用界面、通知、错误提示全部支持中文 / 英文，跟随系统语言
- **应用内语言切换**：设置 → 语言（跟随系统 / 中文 / English），切换后重启立即生效
- **README 与 GitHub Pages 中英文**：落地页支持语言切换，英文模式使用英文截图

### 优化

- 正式版移除「发送测试通知」按钮（保留在 Debug 构建）
- 设置页版本号动态显示（不再写死 0.1.0）

### 质量

- 单元测试 31/31 通过

## [0.1.3] - 2026-08-04

### 新增

- **最近 7 天用量趋势图**：弹出面板加油包下方新增按天堆叠柱状图（输入 / 缓存 / 输出），与千问工作台「用量详情」统计口径一致；点击柱子查看单日总量、缓存命中率、输出明细
- 趋势区状态完善：加载中 / 加载失败（展示原因）/ 暂无数据 三态区分
- **全新应用图标**

### 修正

- 兼容网关双成功标记（code==SUCCESS / success==true），修复趋势接口随机「解析错误」
- 限额文案「重置于」改为「重置时间」，明确表示下次重置时间

## [0.1.2] - 2026-08-04

### 优化

- **菜单栏胶囊双百分比**：显示为 `QW 5h剩余% | 7d剩余%`，一眼区分两个周期的剩余量，消除单百分比歧义
- **修复条状图语义**：弹出面板的进度条此前按「已用比例」填充（剩余 99% 时条只有 1%），现改为按「剩余比例」填充，与数字及千问工作台官方页面一致

## [0.1.1] - 2026-08-04

### 修复与优化

- **修复刷新不即时**：网络环境变化（代理配置重载、节点切换、Wi-Fi 切换等）导致连接失效后，点击刷新现在会重建连接池并立即拉取最新数据
- **失败可见性**：数据更新失败时，菜单栏显示「!」、弹出面板显示橙色错误横幅，不再静默展示旧数据
- **菜单栏胶囊更紧凑**：状态项宽度按内容实测，占位宽度减半
- 「更新于」时间精确到秒

### 其他

- 新增 17 个单元测试（FailureTracker / 轮询服务 / API 解析 / 数据模型）

---

## 如何维护

1. 每次发布新版本前，先在顶部 `[Unreleased]` 下记录本次更新内容（分类：`新增` / `优化` / `修复` / `其他`）
2. 发版时把 `[Unreleased]` 改为 `[版本号] - 日期`，并新建空的 `[Unreleased]`
3. 将对应版本的更新内容复制到 GitHub Releases 发布说明，**中英双语**：中文为默认正文，英文放在 `<details>` 折叠块里一键展开（GitHub Releases 不支持自动切换语言，这是最接近"切换"的做法）
4. 可用 `gh release create --notes-file` 或 `gh release edit --notes-file` 写入
